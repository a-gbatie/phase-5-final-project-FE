import React from 'react'

const HomePage = ({ username }) => {
  return <div className='home'> Welcome {username}! To start creating dates please log into your account, or create a new one with signup.</div>
}

export default HomePage;